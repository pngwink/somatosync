# Optional: set this to the deployed SomatoSync origin so Recovery Relay QR codes
# can be scanned from another device during demos. Do not include a trailing slash.
VITE_PUBLIC_APP_URL=https://your-somatosync-domain.example
